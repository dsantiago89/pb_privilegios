// registro nuevo de la Tabla Usuariosvar usu1 = new ds.Usuarios();// se inserta los valores a los atributos user,pwd y ID_grupo// ** sustituye los valores de los atributos **var usuario="Paco";usu1.user=usuario;var password="123";usu1.pwd=password;var grupo=1; // 1 , 2 o´ 3usu1.ID_grupo=grupo;// ** IMPORTANTE ** se guarda el registrousu1.save();// segun el id de grupo se le asigna un gruposwitch (grupo){    case 1:        grupo = ['Director'];        break;    case 2:        grupo = ['Gerente'];        break;    case 3:        grupo = ['Empleado'];}// es necesario añadir un usuario wakanda para asignarle un grupovar new_usuario = directory.addUser(usuario,password);new_usuario.putInto(grupo);// se guarda los cambiosdirectory.save();